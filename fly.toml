app = 'syndicate-swarm'
primary_region = 'bom'

[build]
  dockerfile = 'Dockerfile'

[env]
  PYTHONUNBUFFERED = '1'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
  auto_stop_machines = 'off'
  auto_start_machines = true
